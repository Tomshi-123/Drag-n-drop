/* function TaskModal({ task, onClose }) {

  return (
    <div className="modal">
      <h2>{task.title}</h2>
      <p>{task.description}</p>
      <input type="text" placeholder="Uppdatera titel" />
      <textarea placeholder="Uppdatera beskrivning"></textarea>
      <button>Spara</button>
      <button>Ta bort</button>
      <button onClick={onClose}>Stäng</button>
    </div>
  );
}

export default TaskModal; */